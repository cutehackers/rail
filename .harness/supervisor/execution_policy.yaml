version: 2

artifacts:
  root: .harness/artifacts

runtime:
  provider: python_actor_runtime
  validation_profile: standard

validation:
  commands:
    - uv run --python 3.12 pytest -q
    - uv run --python 3.12 ruff check src tests
