# Archive Notes

Files in this directory are retained as historical release records and evidence snapshots.

They may reference earlier runtime shapes such as the old Dart CLI, `bin/rail.dart`, or pre-Go operator commands because they capture what existed at that point in time.

Use these files only as archive material.
Do not treat them as the current Rail product contract or active operator guidance.
