name: rail
description: Control-plane harness project for Codex-driven Flutter and Riverpod work.
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
