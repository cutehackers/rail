priority_focus:
  - Preserve bounded supervisor routing.
missing_requirements:
  - Ensure validation targets match the requested behavior.
risk_hypotheses:
  - Environment access may be unavailable.
validation_expectations:
  - Capture environment failures explicitly in evaluator outputs.
generator_guardrails:
  - Avoid changing files outside the bounded request.
blocked_assumptions:
  - Target workspace permissions may block execution.
